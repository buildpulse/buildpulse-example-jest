FROM vm/ubuntu:18.04

RUN curl -sL https://deb.nodesource.com/setup_16.x | bash && \
    apt install nodejs && \
    rm -f /etc/apt/sources.list.d/nodesource.list

COPY . .

RUN npm ci

SECRET ENV BUILDPULSE_ACCESS_KEY_ID
SECRET ENV BUILDPULSE_SECRET_ACCESS_KEY

RUN bin/ci-test
